name: API-Lib-CI/CD

on:
  push:
  workflow_dispatch:

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/back-lib-cicd.yml@v3.1.1
    with:
      cicd_version: v3.1.1
    # nosemgrep: yaml.github-actions.security.secrets-inherit.secrets-inherit -- the called workflow is our own centralized CICD
    secrets: inherit
